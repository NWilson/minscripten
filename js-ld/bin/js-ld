#!/bin/sh
SCRIPT_PATH=`readlink -f "$0"`
SCRIPT_DIR=`dirname "$SCRIPT_PATH"`
JAR="$SCRIPT_DIR/js-ld-1.0-SNAPSHOT.jar"

case `uname -s` in
CYGWIN*) JAR=`cygpath -w "$JAR"`;;
esac

java -jar "$JAR" "$@"

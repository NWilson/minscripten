// Our external library is a simple jQuery-lookalike to illustrate how you'd
// call something like jQuery from js-ld code.
function $(query) {
  return [{value: 123}]; // Node.js fallback, just for testing...!
}
$.isArray = Array.isArray;
module.exports = $;
